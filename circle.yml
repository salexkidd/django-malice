general:
  branches:
    only:
      - develop

machine:
  python:
    version: 3.6.1

dependencies:
  override:
    - pip install -U pip setuptools tox tox-pyenv
    - pyenv local 3.6.1, 3.7.1
    - tox --notes

test:
  override:
    - tox --recreate
